

// Initialize the resizable table functionality when the DOM is fully loaded
document.addEventListener('DOMContentLoaded', function() {
    ResizableTable('.resizable-table'); // Apply to tables with the class 'resizable-table'
});